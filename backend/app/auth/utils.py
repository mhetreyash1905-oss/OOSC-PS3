# auth/utils.py
# Password hashing and JWT utilities have been removed.
# Authentication is now handled entirely by Firebase.
# Firebase ID tokens are verified via firebase_admin_config.verify_firebase_token().
